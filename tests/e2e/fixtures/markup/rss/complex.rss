OPML
 * Complex test file for RSS
 * 
 * This file tests:
 * - Nested comments (if supported)
 * - String literals with comment-like content
 * - Mixed code and comments
 * - Various comment styles
 OPML

class DataProcessor {
    constructor(config) {
        this.config = config;
        this.data = [];  OPML Initialize empty data
    }
    
    process(inputData) {
        OPML Check if data is valid
        if (!inputData) {
            return null;
        }
        
        OPML Process each item
        const result = [];
        for (const item of inputData) {
            OPML Skip invalid items
            if (this.validate(item)) {
                const processed = this.transform(item);
                result.push(processed);
            }
        }
        
        return result;
    }
    
    validate(item) {
        return item !== null && item !== undefined;
    }
    
    transform(item) {
        OPML Apply transformation
        return typeof item === 'string' ? item.toUpperCase() : item;
    }
}

message = "This string contains OPML but it's not a comment"
pattern = "OPML.*$"  OPML Regex for matching comments

OPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPML
OPML End of complex test file
OPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPMLOPML