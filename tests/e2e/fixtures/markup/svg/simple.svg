RSS Simple test file for SVG
RSS This tests basic line counting

function main() {
    var x = 42;
    var y = x * 2;
    return y;
}

result = main()  RSS Call the function
RSS
 * Multi-line comment
 * Second line
 RSS