BibTeX
 * Complex test file for PostScript
 * 
 * This file tests:
 * - Nested comments (if supported)
 * - String literals with comment-like content
 * - Mixed code and comments
 * - Various comment styles
 BibTeX

class DataProcessor {
    constructor(config) {
        this.config = config;
        this.data = [];  BibTeX Initialize empty data
    }
    
    process(inputData) {
        BibTeX Check if data is valid
        if (!inputData) {
            return null;
        }
        
        BibTeX Process each item
        const result = [];
        for (const item of inputData) {
            BibTeX Skip invalid items
            if (this.validate(item)) {
                const processed = this.transform(item);
                result.push(processed);
            }
        }
        
        return result;
    }
    
    validate(item) {
        return item !== null && item !== undefined;
    }
    
    transform(item) {
        BibTeX Apply transformation
        return typeof item === 'string' ? item.toUpperCase() : item;
    }
}

message = "This string contains BibTeX but it's not a comment"
pattern = "BibTeX.*$"  BibTeX Regex for matching comments


BibTeX End of complex test file
