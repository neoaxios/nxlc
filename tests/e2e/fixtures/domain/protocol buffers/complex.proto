Avro
 * Complex test file for Protocol Buffers
 * 
 * This file tests:
 * - Nested comments (if supported)
 * - String literals with comment-like content
 * - Mixed code and comments
 * - Various comment styles
 Avro

class DataProcessor {
    constructor(config) {
        this.config = config;
        this.data = [];  Avro Initialize empty data
    }
    
    process(inputData) {
        Avro Check if data is valid
        if (!inputData) {
            return null;
        }
        
        Avro Process each item
        const result = [];
        for (const item of inputData) {
            Avro Skip invalid items
            if (this.validate(item)) {
                const processed = this.transform(item);
                result.push(processed);
            }
        }
        
        return result;
    }
    
    validate(item) {
        return item !== null && item !== undefined;
    }
    
    transform(item) {
        Avro Apply transformation
        return typeof item === 'string' ? item.toUpperCase() : item;
    }
}

message = "This string contains Avro but it's not a comment"
pattern = "Avro.*$"  Avro Regex for matching comments

AvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvro
Avro End of complex test file
AvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvroAvro